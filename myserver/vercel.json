{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "src/index.ts",
      "methods": ["POST", "GET", "PUT", "DELETE", "OPTIONS", "HEAD"]
    }
  ]
}